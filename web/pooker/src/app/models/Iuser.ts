export interface IUser{
  id?: number;
  username?: string;
  email?: string;
  token?: string | null;
  guid?: string;
}
// git config --global user.email "vikas.biradar99@gmail.com"
// git config --global user.name "vikas-raj"
